# 学习进度

最后整理：2026-09-17。只记录已确认的结果，创建文件或安装依赖不等于掌握知识。

## 第 22 课前交互检查与课程创建（2026-09-17）

- 修正：重复点击当前会话不再撤销文件快照；没有 trace 的消息不显示空的“处理记录”；附件发送后可在“＋”中看到仍用于追问的文件数量，并通过“清除会话文件”撤销。
- 保留现状：发送后清空待发送附件，当前快照仍可用于追问；新选文件成功后替换快照，取消选择保留原状态；终端入口仍隐藏。
- 本轮验证：Node/Web 类型检查、构建、修改文件 ESLint 和隔离 Electron 工作台回归通过；查看了 680×560 窄窗口截图。测试采用模拟业务接口，未调用真实模型或启动真实终端。
- 后续可优化但不阻塞上课：消息复制与失败重试、每个会话独立草稿、已发送消息的附件清单展示。涉及记录结构的改进另行安排，避免与差异审查一起扩大范围。
- 已创建[第二十二课：修改建议与差异审查](32-第二十二课-修改建议与差异审查.md)，包含 Vue 对照、实际路径、分步练习、关键片段、验收和八题自测。状态为“课程已创建，待亲手实现”，不记录为已完成。
- 第 22 课先完成单文件只读预览；真实模型建议、确认写入和冲突保护后续接入。无需先做目标、计划、绘图标签。

## 阶段状态

| 阶段 | 状态 | 已知证据 / 下一步 |
|---|---|---|
| 环境与骨架 | 骨架已创建 | 已读取 package.json 与源码；React、TS、Electron 配置存在 |
| 0：入口与通信 | 理论学习与自测反馈完成，实践证据待补齐 | 用户确认调试正常；已理解接口暴露、类型声明与静态检查；pong 日志或断点命中结果尚未单独报告 |
| 1：React 聊天界面 | 第二、三课完成（用户反馈） | 已核对消息状态、ChatInput、回调 Props 和卸载实验；本次未运行交互与类型检查 |
| 2：类型化本地接口 | 第四课完成（用户反馈），类型检查通过 | 用户确认没问题；本次 Node 和 Web 检查均通过，交互未独立重跑 |
| 3：模型与流式聊天 | 第九课完成（用户反馈），自测继续巩固 | 正式流消息及终态源码已核对，两组类型检查通过；旧练习区已移除挂载，实际交互未独立验收 |
| 4：持久化 | 第十四课完成（用户反馈），源码收尾已核对 | 会话 ID 传递与滚动已补齐，Node/Web 类型检查通过；自动保存仍为 10000ms，真实交互专项未逐项独立验收 |
| 消息展示补充 | 第十五课源码已接入，交互待验收 | 依赖、Markdown 组件与角色分支已核对；Node/Web 类型检查通过，真实排版、安全行为和重启待验收 |
| 5：本地终端 | 第十七课完成（用户反馈），原理待巩固 | Spectre 安装、node-pty 重建、面板 ESLint 和 Node/Web 类型检查此前通过；自测 1–8 已讲解，真实交互及 PID 清理专项未在本轮独立重跑 |
| 6：SSH | 未开始 | 需要有权使用的测试主机 |
| 最小 AI 工具循环 | 第十八课完成（用户反馈），原理待巩固 | 正式循环与时间工具源码已核对，自测 1–8 已讲解；本轮未独立重跑类型、窗口或真实模型专项 |
| 正式聊天整合 | 第十九课完成（用户反馈），自测已讲解 | 已核对模式入口、工具进度、停止路由、App key 和终端；旧草稿重置 Effect 已清理，交互未独立重跑 |
| 工具历史保存恢复 | 第二十课完成（用户反馈），自测已讲解 | 版本 3、历史选择、原子提交、持久 trace 和容量预检查已接入；本轮类型检查通过，未独立重跑窗口验收 |
| 项目文件工具 | 第二十一课完成（用户反馈），自测已补充讲解 | 已有第 1–8 步实现和离线回归记录；八题原理待复述，真实模型与窗口专项未在本轮独立重跑 |
| 7：MCP | 未开始 | 从只读教学工具开始 |
| 8：打包与自动更新 | 未开始 | 需要测试更新源和两个版本 |
| 9：参考项目复盘 | 未开始 | 对照一个模块并改进 |

## 附件添加即授权（2026-09-17）

- 用户明确要求“添加文件就代表允许发送文件”。已移除输入框中的发送许可勾选框，以及前端独立的许可 state、setter 和重置逻辑。
- 真实请求根据当前已选附件自动携带发送许可，模拟请求仍不联网。选择文件本身不触发模型请求；用户发送问题后，模型按需读取获准快照。
- 成功增删文件继续生成新快照并隔离旧上下文；剩余附件不需要重复授权。移除全部、新建/切换会话、刷新与关闭仍撤销当前授权，主进程继续验证窗口、会话和快照归属。
- 现有工作台回归同步改为检查“无复选框、添加后直接发送”，下面的早期记录保留当时的设计过程；当前规则以本节为准。
- 本次修改的 ESLint、Node/Web 类型检查、构建和隔离 Electron 工作台回归均通过；回归确认添加附件后直接携带当前快照发送，模拟来源仍不授予网络发送标记。

## 统一聊天输入框与附件浮层（2026-09-17）

- 用户要求第 22 课前统一普通聊天、时间工具和项目文件入口，并进一步提供截图纠正浮层：应与整个输入框同宽、贴输入框上沿展开，附件应显示在输入框内部。
- 当前默认使用真实模型工具循环：无附件时可直接回答或查询时间，有附件时增加搜索和读取工具。模拟与原纯文本流式入口移到“＋ → 开发调试”。统一入口目前显示进度后提交完整答案，未新增工具协议流式解析。
- `ComposerAttachments` 改为原生 Popover：左右边缘与输入框对齐，上沿之外间隔 8px，菜单打开不改变聊天区和输入框位置；支持 Esc、点击外部和关闭按钮。`AttachmentCards` 在文字输入区上方展示横向文件卡片、同名区分和单独移除，菜单可全部移除。真实发送许可在输入框工具栏中。
- 直接打开一次多选文件对话框，可从不同目录分次添加；相同源文件重新选择会刷新其内容。每个附件使用独立标识，主进程保留源路径用于去重，不把绝对路径传给页面或模型。仍只支持文本和代码，图片缩略图输入尚未接入。
- 新增 `project:remove` 类型化接口。成功增删附件产生新 snapshotId 并重置发送许可，取消或失败保留先前完整授权；新旧快照历史隔离。主进程任务和授权统一使用 BrowserWindow.id，修复原先混用 webContents.id 的隐患；选择和任务相互排斥。
- 工具执行和协议解析共用白名单，项目范围可组合时间和文件工具；循环预算为 5 次模型请求、4 次工具执行和 90 秒。会话库结构仍为版本 4，旧记录可读。
- Node/Web 类型检查、修改文件 ESLint、构建和离线附件集成检查通过。检查覆盖直接多选、同名文件、快照更新、大小与编码、链接/硬链接拒绝、混合工具、无授权工具拒绝、历史范围、窗口/会话归属、取消保留和迟到选择清理。
- 已用正式 Electron UI/preload 与内存业务替身验证菜单同宽且与输入框上沿对齐、附件卡片位于输入区上方、浮层不改变布局、Esc/外部关闭、许可不足保留草稿、增删与调试切换，以及原聊天保存/取消/关闭保护。已查看桌面和 680×560 加终端截图。
- 使用合成文件和临时目录，未调用真实模型、未启动真实 PowerShell；原生选择器由替身核对单次多选配置，未代替用户完成真实文件端到端操作。没有读取本次参考截图附带的账户 JSON 内容。
- 最新用法与路径见[输入框交互整合](31-输入框交互整合-统一聊天与附件浮层.md)和[项目结构地图](02-项目结构地图.md)。下一步仍为第 22 课修改建议与差异审查。

## 第 21 课后的工程整理与工作台布局（2026-09-17）

- 已按用户要求完成[第二次工程整理](30-工程整理-项目上下文与工作台布局.md)，更新[项目结构地图](02-项目结构地图.md)与导航。
- 前端提取 `capacity.ts`、`chat-mode.ts`、`useProjectSelection`、`ChatModeSelect` 和 `ProjectPanel`；`useModelStream` 更名为 `useChatRequest`，普通聊天与工具任务继续共用归属、停止和终态处理。任务启动时捕获模型模式，项目 Hook 不向外暴露内部 ref。
- 项目文件详情默认折叠，保留文件数量和授权状态；真实发送许可在折叠后仍可见，展开后可查看文件、选取时间和撤销入口。详情限高滚动，短窗口压缩输入区，终端高度随视口调整。
- `main.css` 改为样式入口，具体规则拆到工作台、聊天、项目、Markdown、弹窗、终端和响应式文件。已修正选择或允许发送后旧错误提示残留的问题。
- 验证通过：Node/Web 类型检查、构建、修改模块 ESLint、窗口控制检查；扩展原有 Electron 工作台回归后通过，覆盖原聊天/保存/取消/退出保护，以及项目许可不足保留草稿、选择取消清空许可、模式切换撤销、请求模式与会话归属、取消历史不续用。
- 已检查 1280×840 展开文件面板和 680×560 折叠面板加终端的实际截图；窄窗口发送按钮可见、无页面横向溢出。截图位于 `.ui-check/workspace-project-expanded.png` 与 `.ui-check/workspace-project-narrow-terminal.png`。
- 隔离窗口需在正常桌面权限下运行；受限环境会出现 GPU 进程启动失败，桌面环境复跑通过。UI 回归使用正式 preload 与合成文件元数据，工具返回本地模拟取消结果；未访问真实项目、调用模型或启动真实 PowerShell，不新增这些专项的验收结论。
- 下一步：第 22 课从新工程基线接入修改建议与差异审查，写入授权另行设计。

## 第二十一课反馈与第十一至二十一课复盘（2026-09-17）

- 用户确认第二十一课完成，表示八道自测不会，要求讲解并总结第 11–21 课主要技术栈。
- 已在[第二十一课第 16 节](28-第二十一课-项目文件读取与搜索.md#16-八道自测题讲解)补充八题答案：快照更新、四种 ID、路径检查、历史范围、记录与授权、JSON 结果限长、文件内容与指令边界、停止与撤销及迟到选择保护。
- 已创建[第十一至二十一课小结](29-第十一至二十一课小结.md)，逐课回顾，并梳理 React、TypeScript、Electron、Node.js、Markdown、xterm、node-pty、PowerShell 和模型工具协议，以及状态归属、清理、迁移和运行时校验。
- 本轮为学习文档整理，未修改应用源码或重跑窗口、真实模型与构建验收；完成状态来自用户反馈，原理仍需自行复述巩固。
- 下一步：复述快照、历史、授权的区别，再进入修改建议与差异审查。

## 第二十课反馈与第二十一课创建（2026-09-16）

- 用户反馈第二十课完成，八道自测不会；已在对话中逐题讲解 trace/协议、单一状态源、按轮存储、不自动重放、原子提交、版本迁移、失败恢复和运行时校验。理解仍待自己复述。
- 本轮核对正式工具循环、IPC、历史解析、版本 3 会话库、工作台、操作互斥与关闭保护；旧草稿重置 Effect 已删除。现有 npm run typecheck 的 Node/Web 两组通过。
- 已创建 [第二十一课：项目文件读取与搜索](28-第二十一课-项目文件读取与搜索.md)，直接接入正式聊天，不恢复旧独立文件练习面板。采用分步练习、关键片段、验收表与八题自测。
- 课程设计新增只读选中文件快照、按窗口与会话绑定的授权、搜索和读取工具，复用原工具任务生命周期。计划版本 4 增加 ToolRun.scope，按模式和快照隔离历史；普通聊天不跨过项目轮次携带旧文件答案。
- 选择不等于向模型发送；真实项目模式需明确发送许可。快照/授权不持久化，保存的工具结果可含文件片段，重启只恢复记录而不恢复权限。
- 本轮只修改学习文档，未改 src、依赖、锁文件或真实会话记录；未独立操作窗口、选择/上传项目文件或调用模型。新课程的完整实现、迁移、路径与窗口生命周期测试仍待学习者完成，不能套用旧草稿的检查结论。
- 文档检查通过：学习目录 89 个本地 Markdown 链接有效、代码围栏配对；本课 7 个 TypeScript 片段语法检查通过（接口成员和 await 片段补外层后检查），sameToolScope 的 5 个范围比较案例通过。上述不代表完整课程实现已经类型检查或运行验收。
- 下一步：按第二十一课先完成临时目录快照与两个纯工具，再做版本迁移与正式聊天接线，最后按验收表验证。

## 第二十一课第 1 步完成记录（2026-09-16）

- `src/shared/project.ts` 已补充 `parseToolScope` 与 `parseProjectSelectionResult`，不再只依赖 TypeScript 类型。
- 范围解析区分 `time` 与 `project`，项目范围使用现有 `isAgentId` 规则校验 `snapshotId`；`time` 明确拒绝多余的 `snapshotId`。
- 选择结果校验目录标签、ISO 时间、1–8 个唯一相对路径、允许的文本扩展名、隐藏/敏感/阻断路径段、单文件与总字节数、行数及错误文字上限；Windows 路径重复按不区分大小写处理。
- 解析成功后深拷贝文件清单，失败返回 `null`，没有把 `as` 类型断言当作运行时校验。
- `npm run typecheck`、`npx eslint src/shared/project.ts` 和格式化检查通过；针对合法范围、重复路径、路径遍历、超大文件和错误文字上限的 8 个内联案例通过。日期解析还拒绝无效日历日期，不把运行时自动归一化当作合法时间。
- 本步未读取磁盘文件、未新增 IPC、未调用模型；下一步实现主进程只读快照。

## 第二十一课第 2 步完成记录（2026-09-17）

- 新增 `src/main/tools/project-snapshot.ts`，实现 `createProjectSnapshot(directory, selected, signal)`。
- 选择阶段解析真实根目录，使用 `relative` 检查越界；逐段 `lstat` 和最终 `realpath` 拒绝符号链接、目录联接、跳出根目录的路径；普通文件还需保持单链接。
- 读取阶段先限制单文件 32768 字节，再通过只读文件句柄核对设备、inode、大小、修改时间和变更时间；循环读取最多 32769 字节，检测选择期间的文件变化并在 `finally` 关闭句柄。
- 只接受本课规定的文本扩展名和路径段；总大小限制 128 KiB，使用 fatal UTF-8 解码，拒绝低位控制字符，统一 CRLF/CR 为 LF 并保存为只读行数组。
- 全部文件成功后才生成新的 `snapshotId`、目录显示名、创建时间和文件清单；修改磁盘文件不会改变已创建的内存快照。
- `npm run typecheck:node`、该文件 ESLint 和 Prettier 检查通过；临时目录 7 项内联检查覆盖正常读取、CRLF 行拆分、快照不变、越界和 32769 字节拒绝。
- 本步未接入搜索/读取工具、IPC、模型或真实窗口；下一步实现绑定快照的两个纯工具执行器。

## 第二十一课第 3 步完成记录（2026-09-17）

- `src/main/tools/project-snapshot.ts` 已增加 `projectTools`、`createProjectExecutor` 和 `ProjectExecutor` 类型，工具名固定为 `search_project_text` 与 `read_project_file`。
- 搜索只遍历快照中的行数组，使用区分大小写的字面量 `includes`，按路径和行号排序，最多返回 20 个匹配；片段最多 200 字符，省略或截断都会标记 `truncated`。
- 读取只接受清单中的精确路径，行号从 1 开始且一次最多 80 行；输出保持完整行，若结果上限导致删去后续完整行则调整 `endLine` 并标记截断，第一行无法完整放入时返回错误。
- 两个工具都严格解析 JSON、拒绝多余字段和非法参数；结果在 `JSON.stringify` 后限制在 12000 字符，不对序列化字符串直接截断。
- `npm run typecheck:node`、该文件 ESLint 和 Prettier 检查通过；临时快照的 9 项执行器检查通过。
- 本步未接入工具循环、IPC、真实模型或界面；下一步处理版本 4 的工具范围和历史迁移。

## 第二十一课第 4 步完成记录（2026-09-17）

- `ToolRun` 已增加必填 `scope`；`sameToolScope` 区分时间工具、同一项目快照和不同项目快照。
- `parseProtocolTurn`、`parseToolHistory`、`parseAgentResult` 已支持 scope；时间范围只接受 `get_current_time`，项目范围只接受 `search_project_text` 和 `read_project_file`。
- 版本 3 解析器保留为严格旧格式，只验证时间工具且不接受 scope；新增 `migrateV3` 将合法旧轮次补为 `{ kind: 'time' }` 并生成版本 4。版本 1/2 先形成合法版本 3，再复用该迁移路径。
- `parseLibrary` 现在只接受完整版本 4，缺少 `toolRuns` 或 `scope` 的数据会被拒绝；消息、trace、items、会话数量和快照容量限制继续保留。
- 会话存储的源版本已扩展到 1/2/3/4，旧版本加载时先备份原始字节再写版本 4；空库、前端初始状态、新建会话和工作台检查夹具已更新为版本 4。
- 当前时间工具请求会捕获并传递 `{ kind: 'time' }`，历史选择也按 scope 停止，不会把不同项目快照的协议混入同一链。
- `npm run typecheck`、相关 ESLint/Prettier 和 `npm run build` 通过；内联 scope/迁移/备份检查 10/10 通过。尚未接入项目授权 IPC、真实项目工具请求和窗口验收。
- `npm run check:workspace` 的构建和窗口控制检查通过；Electron 工作台因本机 GPU 进程不可用退出，未将工作台交互记作通过。

## 第二十一课第 5 步完成记录（2026-09-17）

- `src/main/agent/tool-loop.ts` 新增 `ExecuteTool` 注入接口；保留原六个参数顺序，在末尾追加执行器和 `ToolScope`，默认异步包装时间工具。
- 循环按范围限制工具名，调用执行器时传递 `AbortSignal`，保留取消、重复 `call_id`、三次模型请求、两次工具调用和输入大小限制；工具结果超过 12000 字符会在交回模型前停止。
- 成功解析按本次范围调用 `parseProtocolTurn`；旧历史只作为输入，不会再次执行。trace 仅记录工具名、call_id 和结果长度，不保存文件正文。
- `src/main/model/tool-response.ts` 已提取 `createLiveResponse(tools, instructions)`；时间入口继续使用原工具、HTTPS 校验、`redirect: 'error'`、响应体上限、`store: false`、reasoning 配置和取消信号。
- `agent-ipc.ts` 已把已校验的 scope 传给循环；项目执行器暂未绑定窗口授权，按课程边界留到第 6 步。
- `npm run typecheck`、`npm run build`、本步三个文件的 ESLint/Prettier 检查通过；离线工具循环回归 5/5、模型响应工厂回归 1/1。
- 本步未读取真实项目文件、未调用模型网关、未新增项目授权 IPC 或窗口交互验收；下一步处理按窗口和会话绑定的项目选择与授权生命周期。

## 第二十一课第 6 步完成记录（2026-09-17）

- 新增 `src/main/agent/project-access.ts`，按窗口保存最多一份项目授权，绑定 `conversationId`、`snapshotId` 和只读快照；选择 token 与 `AbortController` 防止对话框迟到结果覆盖新状态。
- 选择流程使用绑定父窗口的目录/文件对话框，目录和文件均取消、窗口刷新、渲染进程退出或窗口关闭都会让选择失效；快照完整创建后才发布选择结果，页面只收到相对路径、字节数和行数。
- 新增项目选择与撤销 IPC。选择前拒绝正在运行的工具任务和重复选择；撤销先删除授权，再中止使用同一快照的任务；旧快照 ID 或其他窗口不能撤销当前授权。
- `agent:start` 改用运行时校验的 `AgentContext`；项目请求必须匹配当前窗口、会话和快照授权，真实项目请求还必须明确 `allowUpload=true`。任务捕获的快照、范围和执行器在请求期间固定，不读取页面后续状态。
- 项目任务已注入 `createProjectExecutor` 和项目工具 schema；真实项目请求使用带相对路径/行数清单的 instructions，未把绝对路径或文件正文放入 instructions。时间工具请求继续走原有路径。
- `AppAPI`、preload 已增加 `selectProjectFiles`、`revokeProjectFiles`，并按本次上下文解析历史和结果；旧三参数、四参数时间调用仍可用。
- `npm run build`、Node/Web 类型检查、相关 ESLint/Prettier 检查通过；`AgentContext` 边界回归 6/6 通过。
- `npm run check:workspace` 的构建和窗口控制检查通过；Electron 工作台交互阶段因本机 GPU 进程不可用退出，未将正式窗口交互记作通过。
- 本步未新增正式工作台项目模式、许可复选框或真实窗口选择验收；这些属于第 7 步，真实文件和模型网关也未调用。

## 第二十一课第 7 步完成记录（2026-09-17）

- `useOperation` 新增 `selecting` 状态；新建、切换、清空、保存、发送和选择/撤销授权共享互斥检查，关闭保护能提示正在选择文件。
- `ChatMode` 新增 `project-mock` 与 `project-live`。`useConversation` 管理当前快照、会话归属和 `allowUpload`，切换会话、离开项目模式、刷新和卸载都会撤销授权并清理许可。
- `useModelStream` 在发送前检查项目选择、会话/快照归属和真实发送许可，按快照与模式选择历史；普通聊天会在最近项目工具轮次后重建文本上下文，避免携带项目答案。
- `ChatWorkspace` 显示项目文件相对路径、字节数和快照信息，提供重新选择、撤销及真实模式许可复选框；`ChatInput`、侧栏和清空对话组件已支持动态模式及异步互斥操作。
- `npm run typecheck`、`npm run build`、本步相关 ESLint 和 Prettier 检查通过。`npm run check:workspace` 的构建和窗口控制检查通过；Electron 工作台阶段因本机 GPU 进程不可用退出，未将正式窗口交互记作通过。
- 项目模拟模式已接入授权和上下文，但第 8 步的 `project-response.ts` 尚未实现，因此当前仍会命中原时间工具，因 scope 不匹配而失败；这不作为第 7 步模拟回答验收结果。
- 本步未读取真实项目文件、密钥或调用真实模型服务。下一步实现项目搜索、读取、回答模拟器，再进行两轮模拟问答和保存重启验收。

## 第二十一课第 8 步完成记录（2026-09-17）

- 新增 `src/main/model/project-response.ts`，实现 `createProjectMock(selection)`。每个任务在闭包内生成两个独立的 `call_id`，不根据包含历史的 input 长度判断当前步骤。
- 模拟响应固定先调用 `search_project_text({ query: 'greet' })`；校验最新的搜索结果后优先读取源码命中，按清单行数生成最多三行的 `read_project_file` 调用；读取完成后回答并引用实际相对路径和行号。
- 无搜索命中时直接返回“所选文件未找到 greet”；搜索或读取结果的 call_id、JSON 结构和清单范围不匹配时拒绝继续，不把工具结果当作模型指令。
- `agent-ipc.ts` 已按项目快照和 mock/live 模式选择 `createProjectMock` 或真实响应工厂；项目执行器仍由主进程快照提供，模拟器不会读取磁盘或联网。
- 离线回归通过：一次问答产生 3 次模型请求和 2 次工具调用，答案引用 `src/greet.ts:1-3`；同一快照第二轮使用旧协议但只返回本轮 items 且 call_id 不重复；无命中返回明确提示。
- `npm run typecheck`、`npm run build`、本步相关 ESLint 和 Prettier 检查通过。未调用真实模型、未读取用户项目文件或密钥；Electron 工作台交互仍受本机 GPU 进程不可用限制。
- 下一步进入修改建议与差异审查课程，项目写入和命令执行仍需独立授权与冲突保护。

## 第十九课反馈与第二十课创建（2026-09-16）

- 用户反馈第十九课完成，八道自测不会；已在对话中逐题讲解请求归属、模式与停止、数据分层、互斥、迟到事件、保存边界及终端权限。原理掌握仍待复述。
- 本轮核对源码：App 已移除独立 AgentPractice、恢复终端并为 ChatWorkspace 添加会话 key；模式下拉框、toolActivity、完成记录和停止分流已存在。ChatWorkspace 仍保留旧的草稿重置 useLayoutEffect，第二十课前置检查明确要求删除。
- 已创建 [第二十课：工具历史的保存与恢复](27-第二十课-工具历史的保存与恢复.md)。采用分步练习与关键片段，完整解析器和接线留给学习者；未把课程草案当作已编译通过的完整实现。
- 已读取官方 Function calling 与 Reasoning models 文档，核对完整 output、call_id、reasoning、encrypted_content 和 phase 保留要求。继续沿用现有模型配置，不替换模型。
- 课程规定版本 1/2 先备份迁移至 3；只将成功、连续、同模式的完整工具轮次用于续聊。失败/取消只保存展示记录，中断不自动续跑，旧版本记录不伪造工具历史。
- 本次只修改学习文档，未修改 src、依赖或真实会话文件；未调用模型或独立验收窗口。10 个可独立解析的 TypeScript 片段语法检查通过（不包括需嵌入既有类型的两个片段），历史选择函数通过 9 项内联离线检查，全目录 81 个本地 Markdown 链接有效、代码围栏配对。上述不是完整实现的类型或运行时验收。
- 下一步：先清理前置 Effect，再依次实现纯数据校验、临时目录迁移验证、工具循环历史接口和原子终态提交，最后进行保存重启验收。

## 第二十课第 3 项完成记录（2026-09-16）

- `src/shared/conversation-library.ts` 已升级为版本 3，`Conversation` 增加 `toolRuns`，保留 `parseLibraryV2` 作为旧格式校验。
- 已实现 `migrateV2`、更新 `migrateV1` 和 `readLibrary`；版本 1/2 迁移后只补空工具历史，不伪造旧工具调用。
- `src/main/storage/conversation-store.ts` 已对空库使用版本 3，并根据源版本备份旧文件；`useConversation.ts` 的初始库和新会话已补 `toolRuns: []`。
- 隔离回归通过版本 1/2 迁移、版本 3 缺字段拒绝、版本 2 备份和版本 3 写入；`npm run typecheck`、`npm run build`、相关 Prettier/ESLint 通过。
- `npm run check:workspace` 的窗口控制检查通过，Electron 工作台阶段因本机 GPU 进程不可用退出；未将该窗口检查记作通过。
- 下一步：接入工具循环历史参数、返回本轮 items，并在前端终态中原子提交消息和 toolRuns。

## 第二十课第 4 项完成记录（2026-09-16）

- `src/shared/agent.ts` 的成功结果已增加本轮 `items`，preload 返回时重新校验协议并确认最终文字一致。
- `src/main/agent/tool-loop.ts` 已接收已校验的历史，保留旧协议项、只对当前响应执行工具，成功时只返回本轮新增协议项；输入大小和旧 `call_id` 重复检查保留。
- `src/main/agent/agent-ipc.ts` 已接收第 4 个可选 history 参数，主进程用 `parseToolHistory` 重新校验后才启动任务；`shared/api.ts` 与 `preload/index.ts` 已同步桥接，三参数的历史练习调用仍兼容。
- 内存模拟验证通过：历史按原顺序传入、当前问题追加在末尾、旧工具调用不会重执行、返回 items 不包含旧轮次、重复旧 call_id 会拒绝；多段最终文字的结果校验也已覆盖。
- `npm run typecheck`、相关 ESLint、Prettier 和 `npm run build` 通过；未调用真实模型，前端尚未把 toolRuns 在发送和终态更新中接入。
- 下一步：完成前端发送历史、原子提交消息和 toolRuns。

## 第二十课第 5 项完成记录（2026-09-16）

- `src/main/model/tool-response.ts` 的模拟响应已在每个任务创建一次唯一 `call_mock_${randomUUID()}`，模型调用和工具结果校验使用同一个 ID。
- 模拟最终回答会显示输入中用户问题的轮数，用于观察历史是否传入；它仍固定查询香港时间，不代表真实模型理解了跨轮指代。
- 两个独立模拟响应的 call_id 不重复，调用与结果匹配、历史用户轮数统计、错误 call_id 拒绝均通过内联检查。
- `npm run typecheck` 和该文件 ESLint 通过；真实模型和正式聊天前端尚未接入持久 toolRuns。
- 下一步：实现前端发送前选择历史，以及消息与 toolRuns 的原子终态提交。

## 第二十课第 6 项完成记录（2026-09-16）

- `useModelStream.ts` 发送工具请求前会按当前模式选择尾部连续成功历史；请求开始时一次追加 pending 消息和空 `ToolRun`，并把已捕获的历史绑定到本次请求。
- 工具成功、失败、取消和异常都在同一次会话更新中结束消息并写入 trace/items；半轮协议不会进入后续上下文，临时进度在终态后由持久 trace 接替显示。
- `useConversation.ts` 增加稳定的会话更新入口，清空会话会同时清除 messages 和 toolRuns；展示记录由已保存 trace 与本轮临时进度派生。会话切换由 App 的 key 重建 ChatWorkspace，不再依靠旧的同步草稿 Effect。
- 工具模式说明已写明连续成功的同模式规则、保存后恢复规则以及失败/切换模式的断链规则；MessageList 的执行记录不再标记为临时显示。
- `npm run typecheck`、相关 ESLint、Prettier 和 `npm run build` 通过。尚未把真实模型、保存重启、连续三轮、停止/失败恢复作为窗口验收完成。
- `npm run check:workspace` 中的窗口控制检查通过；Electron 工作台仍因本机 GPU 进程不可用退出，不能把该次工作台检查记作通过。
- 下一步：按第二十课第 13 节在临时副本和 Electron 工作台中验证迁移、连续工具问答、保存重启、模式隔离、清空及保存失败保护。

## 第二十课第 7 项完成记录（2026-09-16）

- `useConversation.ts` 增加发送前容量预检查，按全库统计消息数量、正文长度、工具协议长度和格式化 JSON 的 UTF-8 字节数。
- 工具模式额外预留 1 条 `ToolRun`、128000 字符协议空间；所有模式预留 2 条消息、18000 字符正文和 1000000 字节快照空间，避免本轮生成后才超过版本 3 的解析上限。
- 容量不足时发送返回 `false`，输入框草稿保留，并显示新建会话或整理记录的提示；新建、切换、清空会话及切换模式会清除该提示。
- `npm run typecheck`、相关 ESLint 和 `npm run build` 通过；容量临界场景仍需在窗口中手动构造数据验收。
- 下一步：按第二十课第 13 节验证容量拒绝不会写入半轮记录，并继续做保存重启和连续工具问答验收。

## 第十九课调整：工具循环接入正式聊天（2026-09-16）

- 已更新 [第十九课：工具循环接入正式聊天](26-第十九课-工具循环接入正式聊天.md)，统一输入框、工具进度、最终答案和停止入口，保留普通流式聊天与手动 PowerShell。会话切换通过 ChatWorkspace 的 key 重置局部草稿，避免在 Effect 中同步 setState。
- 工具模式本课只发送本轮问题；会话库仍保存问题、最终文字与状态，临时执行记录不持久化。第二十课再设计完整工具协议历史保存恢复，第二十一课适配文件工具草稿。
- 10 个整合示例文件在 .ui-check/lesson19-chat-review 中通过 ESLint，Node/Web 类型检查均为零诊断。正式 src、依赖与锁文件未修改，未新增测试文件。
- 内联离线检查通过：循环进度与 trace 一致、工具结果协议回传、取消；聊天 Hook 的长度限制、重复发送拒绝、进度请求归属、工具停止路由、终态后解锁、成功/失败状态和普通流式回复。Hook 使用轻量 React/API 替身，不能替代真实 React 生命周期、Electron 窗口及模型验收。
- 未读取密钥、未调用真实模型、未修改真实聊天记录。课程仍待学习者亲手实现，并按第 12 节完成窗口、保存重启、刷新和真实模型检查。

## 原文件工具课程创建记录（已调整为后续草稿，2026-09-16）

以下为调整前的历史校验，只对应旧文件工具草稿；其中“下一步”已由当前路线替代。

- 已创建 [原文件工具课程（现为后续草稿）](后续草稿-项目文件读取与搜索边界.md)，同步导航、路线、地图与后续课程安排。正式 src、依赖、锁文件未修改；未调用真实模型、未读取密钥或实际用户项目文件。
- 已核对当前第十八课正式模块与 App。App 的终端 JSX 已注释，导入和 state 尚未清理，课程列出最小收尾步骤，隔离副本按隐藏终端方案处理，不替用户修改正式实现。
- 示例提取到 .ui-check/lesson19-review：5 个新增完整文件及 6 个修改文件接线并格式化。初次 ESLint 发现控制字符正则规则冲突，已改为字符码判断并同步课程；最终 11 个文件 ESLint、隔离副本 Node/Web 类型检查、18 个代码片段语法检查及学习目录 Markdown 文件链接检查均通过。
- 使用临时练习文件和内联执行通过 16 组离线检查：快照与行号、搜索/读取/回答三轮循环、快照更新语义、前缀相似目录越界、绝对路径/UNC/ADS/路径遍历、未选文件、隐藏/敏感名称/编码/二进制/大小限制、总量限制、读取/搜索截断、行号错误、目录联接及硬链接拒绝、取消、窗口归属和上传确认、任务/授权撤销、迟到对话框、导航清理与监听数量，以及第十八课默认时间工具回归。
- IPC 检查使用 Electron 替身，目录联接/硬链接检查使用本机临时文件；未独立验收真实 Electron 对话框、窗口/刷新交互和真实服务。测试没有真实网络调用，未新增测试文件，不能把离线检查记作用户已掌握或全部专项完成。
- 下一步：学习者先准备无敏感内容的小目录，再依次实现循环解耦、快照、桥接和面板；先模拟，后按明确确认发送范围做真实调用。

## 第十八课完成反馈与自测讲解（2026-09-16）

- 用户反馈第十八课完成，自测题不会；八题答案已补入第十八课第 15 节，原理仍待复述巩固。
- 本轮核对正式 src/main/agent/tool-loop.ts 与 src/main/tools/current-time.ts：循环、完整 output 保留、call_id、调用上限及时间工具校验已存在。
- 此前隔离示例检查结果保留；本轮没有重跑正式类型检查、窗口交互、网络取消和真实服务兼容性，不把用户反馈扩大为所有专项独立验收通过。
- 下一步：巩固模型请求、应用执行、结果回传和最终回答的关系，后续顺序现调整为第十九课正式聊天整合、第二十课工具历史、第二十一课文件工具。

## 第十八课创建与示例校验（2026-09-16）

- 已创建 [第十八课：最小 AI 工具循环与只读工具](25-第十八课-最小AI工具循环与只读工具.md)，同步导航、路线、结构地图与后续课程安排。正式 src、依赖、锁文件未修改。
- 已核对当前模型、终端、IPC 和 App 接线；查阅官方 Function calling 与 Reasoning models 文档，课程保留完整 output、phase 和加密推理项，不复用旧纯文本 ModelMessage 历史。
- 六个完整示例按当前 Prettier 配置格式化，ESLint 通过；提取到 .ui-check/lesson18-review 源码副本并补齐 App/API/preload/main 接线后，Node/Web 类型检查通过。未复制密钥配置，未调用真实模型。
- 通过内联临时执行完成九组离线检查：模拟两轮闭环、名称/JSON/时区/额外字段校验、模拟等待取消、完整历史与 call_id 保留、工具错误回传、重复调用拒绝、三次请求/两次工具上限、异常响应拒绝、返回结果解析。未新增测试文件。
- 真实模型兼容性、Electron 窗口交互、IPC 超时/刷新清理和页面布局尚待实践；离线检查不替代这些验收，不标记课程已完成。
- 下一步：学习者按第十八课依赖顺序实现，先模拟、再少量真实查询，反馈执行记录与自测题号。

## 第十七课完成反馈与自测讲解（2026-09-16）

- 用户反馈第十七课完成，自测题不会；八题讲解已追加到第十七课第 13 节，原理掌握仍待复述巩固。
- 此前已安装 MSVC Spectre x64/x86 库，node-pty 1.1.0 针对 Electron 39.8.10 x64 的 install-app-deps 重建成功。
- 此前修正 shared/terminal.ts、local-terminal.ts 格式及 TerminalPanel 初始化 Effect；相关文件分别通过 ESLint，面板修正后 Node/Web 类型检查通过。
- 本轮只核对主进程实现、补充讲解与更新学习记录；未独立重跑窗口交互、启动失败、刷新或关闭后的 PID 检查，不将用户完成反馈记成专项全通过。
- 下一步：巩固输入、输出、退出三条链路，再安排第十八课最小 AI 工具循环；本轮不提前创建新课。

## 第十七课创建（2026-09-16）

- 已创建 [第十七课：真实 PowerShell 与终端进程生命周期](24-第十七课-真实PowerShell与终端进程生命周期.md)，同步导航、路线、结构地图与编写约定。正式应用源码、依赖和锁文件未修改。
- 核对当前 TerminalPanel、App、AppAPI、preload、main 入口及打包配置；环境为 Windows 10.0.26200 x64、Node v24.11.1，实际 Electron 39.8.10，node-pty 未安装。
- 课程覆盖固定 shell/用户主目录、窗口归属、预先确定 sessionId、先订阅后启动、输入输出、尺寸同步、退出码与迟到结果清理；通过独立 PowerShell 查询 PID 验证面板关闭、刷新和窗口销毁。
- 安装重建、完整示例类型检查、Electron 原生模块加载及实际交互均待实践；不把文档创建记为实现或掌握。不新增测试文件。
- 文档检查通过：第十七课 8 个 TS/TSX 片段语法检查（接口和对象片段补外层后检查），学习目录内 Markdown 文件链接检查。语法检查不等于安装 node-pty 后的完整类型检查。
- 下一步：按第十七课先安装依赖，再依次实现协议、主进程、桥接和面板，反馈命令结果、进程清理证据与自测题号。

## 第十六课完成反馈与自测讲解（2026-09-16）

- 用户反馈第十六课完成，自测 1–8 均不理解；实践完成与原理掌握分别记录。
- 已核对 @xterm/xterm、@xterm/addon-fit 依赖、TerminalPanel 输入/输出/清理和 App 同级挂载。当前仍是模拟终端，没有启动真实 PowerShell。
- 本次类型检查发现 TerminalPanel.tsx 尺寸回调内 host 可能为 null（TS18047）；这是课程示例的问题，已将判空后的 DOM 引用另存为非空常量，同步修正正式源码与第十六课。修复后 Node/Web 类型检查通过，未新增测试文件。
- 八题解释已追加到第十六课第 11 节，覆盖三层职责、输入输出方向、Effect、state/ref、容器尺寸、清理、StrictMode 和 Ctrl+C/会话切换。
- 本轮没有独立重跑终端窗口交互，不把完成反馈扩展为所有专项验收通过；原理仍待用户复述巩固。
- 下一步：复习输入去哪、输出从哪来、关闭时如何释放；之后安排第十七课真实 PowerShell 与 PTY，不在本轮提前创建新课。

## 主进程目录整理与第十六课同步（2026-09-16）

- 用户授权小范围结构调整并更新说明和第十六课。main 下新增 window、model、storage 分组，已注释的 mock-stream 历史练习移入 learning；更新正式导入和已有窗口检查路径。
- 清理正式 preload/api 中失效的模拟流注释以及 model 中旧单字符串校验注释；保留非流式 askModel、ping 和独立学习示例。renderer 业务源码未调整，未安装终端依赖。
- 更新 02-项目结构地图.md、根 README、学习导航、路线、编写约定；新增 [第23份迁移说明](23-工程整理-主进程目录分组与课程衔接.md)。第十六课新增整理后基线、终端与 AI 工具的边界及后续模块归属。
- 修改前的 src、scripts、课程和根 README 备份于 .local-backups/before-main-folders-20260916，未备份密钥或真实聊天数据。
- npm run build 通过（包含 Node/Web 类型检查和三端构建）；改动源码及脚本的 ESLint 检查通过。与备份对照编译结果/语法树，确认正式代码除导入路径、注释及格式外的可执行逻辑一致。
- 现有窗口控件检查通过。现有工作台检查修正为版本 2 数据和正式编译 preload，检查窗口的 sandbox 配置与正式窗口一致，等待时间兼容当前 10000ms 自动保存；没有新增测试文件。
- 隔离 Electron 回归通过：正式 preload 桥接、加载保护、流式请求归属、停止、成功上下文筛选、整库保存、失败暂停/重试、忙碌关闭拒绝、已保存关闭放行、清空确认和窄窗口布局。使用内存模型/存储，未调用真实模型、未读写用户聊天文件；不将此记为真实磁盘迁移或系统 Alt+F4 的新增验收。
- 文档本地链接检查通过；第十六课 4 个 TSX 代码片段语法检查通过。xterm 尚未安装，本课完整类型检查与终端窗口效果仍待学习者实践。
- 下一步：先阅读结构地图和迁移表，再按更新后的第十六课亲手实现终端面板。后台任务管理、工具循环及全局退出协调在后续课程学习，不在本次提前重构。

## 第十六课安排与第十五课源码核对（2026-09-16）

- 用户要求创建第十六课；已创建 [22-第十六课-终端界面入门与生命周期.md](22-第十六课-终端界面入门与生命周期.md)，同步目录和学习路线，未代写正式应用核心实现。
- 当前 react-markdown、remark-gfm 已安装；MarkdownContent、MessageList 角色分支存在，未发现临时预览残留。ChatWorkspace 当前是两个 useLayoutEffect；自动保存仍为 10000ms。
- 正式项目 npm run typecheck 的 Node/Web 两组检查通过。未独立执行第十五课的真实窗口、安全展示、流式和重启验收，也未收到本轮完成反馈，不标记已掌握。
- 环境为 Windows x64、Node v24.11.1，实际 Electron 39.8.10。@xterm/xterm、@xterm/addon-fit 尚未安装。
- 第十六课聚焦终端显示层：输入事件观察、模拟输出与停止、FitAddon、ResizeObserver、StrictMode 下的资源清理和工作台面板。第十七课计划连接 node-pty 和真实 PowerShell。
- 本次不新增测试文件、不安装终端依赖、不启动 shell；本课示例的完整类型检查和真实窗口验收留待安装依赖后实践。阶段 5 的真实交互与进程清理尚未实现。
- 下一步：学习者先核对第十五课实际效果，再按第十六课安装依赖、完成面板和接线，反馈验收与自测题号。

## 第十四课收尾与第十五课安排（2026-09-16）

- 用户反馈第十四课完成；此前发现的 conversationId 解构、请求字段与三处更新参数遗漏现已补齐，api.ts 旧类型导入已清理，ChatWorkspace 已保留清草稿及滚动两个 Effect。
- 本次正式项目 npm run typecheck 通过 Node/Web 两组检查；自动保存仍为 10000ms，恢复 800ms 作为课前收尾提示。
- 第十四课八道自测已逐题讲解，不将讲解记作已掌握；真实迁移、切换和流式交互未逐项独立验收。
- 已创建 21-第十五课-消息Markdown展示与安全渲染.md，包含 react-markdown 10、remark-gfm 4 安装说明、独立展示组件、MessageList 接线、局部样式、流式说明与八道自测题。
- 本课只渲染助手消息，保持原始 Markdown 存储和模型上下文；跳过原生 HTML，仅展示 HTTP(S) 链接地址，图片使用占位文字，不自动加载或打开资源。
- 遵循用户“不用做测试文件”的要求，不创建测试文件；用现有页面临时预览、类型检查和实际交互验收。正式应用源码和依赖未代改，第十五课示例尚未安装依赖后编译或做窗口验证。
- 下一步：由学习者完成第十五课，反馈排版与安全行为、清除预览后的流式和重启效果；随后安排本地终端入门。

## 本次学习记录

- 日期：
- 本次目标：
- 修改了哪些文件：
- 实际观察结果：
- 我能解释的原理：
- 尚不理解的问题：
- 报错与解决方法（不记录密钥）：
- 验收是否通过，依据是什么：
- 下一次第一步：

## 第十三课反馈与第十四课安排（2026-09-16）

- 用户反馈第十三课完成，明确自测 1–8 不理解；已逐题讲解单一数据源、四种 ID、活动引用、unknown 校验、稳定 ID、未知版本保护、消息定位与迁移/存储职责，理解程度仍待复述。
- 当前源码已实现 conversation-library.ts，独立编译与正式项目 Node/Web 类型检查通过。scripts/check-conversation-library.cjs 为 0 字节，不能记作用户验收通过；已在新课列明补齐要求。
- 自动保存已补 closePending 依赖，延迟仍为 10000ms；恢复 800ms 留给用户亲手完成。
- 已创建 20-第十四课-多会话存储接入与侧栏切换.md：先备份再迁移落盘、只写版本 2、启动恢复所有 pending、整库 dirty、preload 校验、新建切换、请求绑定会话、清草稿与退出保护回归。
- 将课程示例应用到 .ui-check/lesson14-review 的独立源码副本，Node/Web 类型检查通过；临时目录存储脚本验证缺失文件不落盘、旧版先备份、稳定 ID、保存保留 pending/加载恢复、损坏与未知版本禁止覆盖、队列恢复，全部通过。
- 从第十三课文档提取非空脚本，在副本中配合当前共享模块编译执行通过；这不代表正式 scripts 下的空脚本已补齐。
- 未修改正式应用实现，未读取真实聊天记录，未调用模型。真实窗口的新建/切换、流式隔离、关闭保护和迁移效果待用户实践，不标记为完成。
- 下一步：先完成课前收尾，再按第十四课依赖顺序实现、运行存储检查与项目类型检查，最后反馈交互验收和自测。

## 第十二课完成反馈（2026-09-16）

- 用户确认第十二课完成，Alt+F4 弹窗以及继续编辑、保存并关闭、不保存并关闭均无问题。
- 上一轮源码核对确认主进程拦截、preload 通信与页面 CloseGuard 已接通，实际 Node/Web 类型检查通过。
- 已解释：空闲且已保存时直接关闭；自动保存延迟为 800ms，所以不一定能看到确认框。提示补齐自动保存 Effect 的 closePending 依赖；本次未重新核对是否已补齐。
- 用户反馈自测 1–8 均不知道，需要逐题讲解。实践完成与原理掌握分开记录。
- 保存失败重试、旧请求回复、忙碌关闭等边界未收到逐项实践反馈，不额外标记通过。
- 下一步：巩固关闭调用链、同步拦截、单次放行、保存结果、state/ref 与请求 ID，再安排第十三课多会话数据结构。

## 第十三课安排（2026-09-16）

- 用户要求开始创建第十三课。已核对当前 shared/conversation.ts、main/conversation-store.ts、保存 Hook 和流式消息更新方式；正式项目 Node/Web 类型检查通过。
- 新增 19-第十三课-多会话数据结构与版本迁移.md：版本 2 会话库、活动会话 ID、复用消息校验、旧版纯数据迁移、未知版本拒绝、总量限制和内存验收脚本。
- 从文档提取代码，在隔离目录中严格编译并运行全部示例断言，通过。不代写正式应用实现、不读写真实聊天文件。
- 当前保存计时器仍是 10000ms，自动保存 Effect 仍缺 closePending 依赖，已列入新课课前收尾，由学习者亲手修正。
- 第十二课八题已在对话中讲解，未将讲解等同于用户已掌握；保存失败与过期回复等专项实践仍未逐项报告。
- 下一步：用户完成共享模块和验收脚本，反馈自测；第十四课计划接正式兼容存储、侧栏新建/切换及生成时会话隔离。

## 第十二课安排（2026-09-15）

- 用户确认 UI 布局构建没有问题，要求创建第十二课。以当前自绘标题栏和功能 Hook 为起点，继续由学习者亲手实现。
- 已核对窗口关闭、保存返回类型、操作互斥和会话控制器。当前关闭仍调用 BrowserWindow.close，没有退出保护；save 返回 Promise<void>。本次 Node/Web 类型检查通过。
- 新增 18-第十二课-退出保护与保存后关闭.md：主进程统一拦截、关闭请求 ID、单次放行、关闭期间暂停编辑和自动保存、保存布尔结果、三种退出选择及错误恢复。
- 本课忙碌时取消本次关闭并提示等待，不自动停止生成后退出；草稿持久化、强制退出和多窗口协调不在本课范围。
- 课程示例已接入独立源码副本，Node/Web 类型检查通过；主进程替身验证拦截、重复请求、取消、旧 ID、单次放行、刷新失效、来源检查和清理通过。未修改正式应用实现。
- 隔离 Electron 示例回归通过：使用本课主进程关闭保护和实际页面，模拟模型/内存存储验证忙碌留窗、重复关闭、三种选择、自动保存暂停/恢复、保存失败重试与加载失败关闭。补充关闭后使用已捕获 webContents 引用清理的示例。真实 Alt+F4 与磁盘恢复仍待用户验收。
- 下一步：先阅读新旧关闭调用链，按依赖顺序实现，再反馈三种选择与 Alt+F4、自测及重启恢复结果。

## 自定义窗口按钮（2026-09-15）

- 用户希望标题栏高度变化时，最小化、最大化和关闭按钮的视觉也能统一。移除系统 titleBarOverlay，改为 TitleBar 内的自绘按钮。
- 保留用户设置的 35px 高度，并统一成 base.css 的 --titlebar-height；按钮宽度另有变量，侧栏遮罩同步使用同一高度。
- 新增窗口业务模块和状态 Hook，动作限制为当前窗口的最小化、切换最大化和正常关闭；同步系统最大化状态并清理订阅。
- 类型检查、构建、lint、命令边界/事件清理检查及模拟事件的 Electron UI 回归通过。未操作用户真实窗口或真实聊天数据。

## 工作台简化与标题栏调整（2026-09-15）

- 用户要求移除 Sidebar 顶部品牌/图标及目录选择按钮和事件，优先保留学习框架。
- 新增 TitleBar，侧栏展开/收起按钮固定在窗口标题栏最左；启用系统标题栏控件叠加，空白区允许拖动窗口。
- 删除目录选择的 renderer 回调、状态、preload 方法、共享接口与主进程 IPC；原快照字段保留以兼容旧文件。
- 更新隔离 UI 检查，覆盖移除入口和侧栏按钮位置固定；主进程配置需完整重启后查看。

## 工程整理与工作台布局（2026-09-15）

- 用户提出先整理代码和类似 Codex 的 UI 布局，再继续课程。本次直接实现基础工程调整，不提前创建下一课。
- App 改为组装入口；拆出 useConversation、useOperation、useConversationStorage、useModelStream，以及侧栏、顶栏、消息区和输入组件。旧练习保留在 learning；共享类型不依赖 UI。
- 实现浅色工作台、侧栏折叠与窄屏遮罩、独立消息滚动、固定多行输入区、草稿建议和清空确认。补齐操作禁用，send 返回接收结果以保护草稿。
- 源码统一格式，补齐存储返回类型，整理模拟流 timer 声明；保留旧课程的业务和历史记录。未新增运行依赖。
- Node/Web 类型检查、构建、全项目 lint 通过。隐藏 Electron 窗口中的模拟接口 UI 回归通过，截图已检查；不把它记为真实模型或真实聊天文件集成验收。
- 新增第17份结构导读，更新结构地图、路线和后续编写约定；建议先复习新调用链，再继续退出保护、多会话与消息展示。

## 第十一课反馈与第一至十一课小结（2026-09-15）

- 用户反馈第十一课完成，八道自测均表示不清楚，要求讲解和技术栈小结。
- 已核对 App 与 ChatInput：统一保存入口、800 毫秒防抖、清理、失败暂停和手动恢复已实现，三个函数入口保护已补齐；目录按钮、清空按钮和 ChatInput 的 disabled 仍缺少加载/保存条件。输入框可能在父组件拒绝发送后清掉草稿，已指出具体修正条件，留给学习者修改。
- 实际运行 npm run typecheck，Node、Web 两组通过。未运行窗口、调用模型或读取真实聊天文件，不将自动保存、重试或恢复记为独立验收通过。
- 新增 16-第一至十一课小结与第十一课自测讲解.md，讲解内存与磁盘、防抖与队列、生成终态、闭包依赖、失败暂停、快照签名、清理边界与历史恢复。
- 技术栈总结覆盖 React、TypeScript、Electron、Node.js、IPC、HTTP/SSE、异步与文件持久化，并区分工程工具、编程概念和后续尚未实践的能力。
- 下一步：补齐三个禁用条件，结合八题做断点实验并用自己的话复述；再安排退出确认。

## 第十一课安排（2026-09-15）

- 用户要求开始创建第十一课。重新核对 App、主进程存储、共享 API 和 preload：手动持久化已接入，旧独立流区已移除挂载；三个操作入口及其禁用条件仍未补齐。
- 创建 15-第十一课-防抖自动保存与失败重试.md：起步补齐保护，统一保存入口，空闲 800 毫秒防抖，生成期间不保存，失败暂停自动重试，手动成功后恢复。
- 明确首次加载基线不触发自动写入；清空后自动保存会替换旧记录；pending 恢复规范化不单独写盘；退出确认拆到下一课。
- 本次实际运行 npm run typecheck，Node、Web 均通过；将课程新增状态、保存回调、Effect 及入口检查接入内存中的 App 副本，Web 类型检查通过。未替换应用源码。
- 仅更新学习文档，未调用模型、未读取真实聊天或凭据，未运行 Electron 交互。计时器、失败暂停、手动重试和恢复实验待学习者实践，不提前记为已验收。
- 下一步：从本课第 4 节补齐保护开始，完成自动保存实验并反馈自测，再安排窗口关闭确认。

## 第十课反馈与核对（2026-09-15）

- 用户反馈第十课完成；自测第一至六题表示不清楚，第七题误认为实时保存，第八题正确区分文字与完整状态。
- 核对存储模块、App 读取和手动保存：实现了串行队列、临时文件替换、损坏文件保护和 pending 恢复。npm run typecheck 两组通过。
- 发现课程第 8 节的操作互斥尚未补齐：handleSend、handleClearConversation、handleSelectFolder 没有 storageReady/diskBusy 入口检查；对应 JSX 未增加加载/保存禁用条件。可能在启动加载尚未完成时修改状态，之后被加载结果覆盖。留给学习者亲手补齐。
- 讲解重点：React 内存是当前页面记录；磁盘快照是最后一次手动保存；模型上下文是每次筛选发送的历史。这三者不会自动同步。
- 已讲解：ENOENT 是文件缺失，损坏可能仍含可恢复数据；userData 与工作目录职责不同；临时文件替换保护旧版本但不是数据库事务；队列串行执行且失败后可继续。
- 已纠正：pending 不能恢复网络任务；清空未保存后旧聊天回来是磁盘快照未变，不是实时保存；消息文件不含完整推理状态和密钥。
- 本次未读取真实聊天文件、调用网关或运行保存/恢复 UI 验收。下一步先补齐第 8 节并用保存—清空—重启实验巩固，再考虑自动保存。

## 第十课安排（2026-09-15）

- 用户要求创建第十课。核对共享 API、Message 类型、App 状态与后续约定；当前尚无本地持久化，旧真实流练习区仍挂载。
- 创建 14-第十课-聊天记录的本地保存与恢复.md：共享快照类型与校验、固定 userData 文件、串行队列、临时文件 rename、读取失败阻止覆盖、启动恢复与手动保存。
- 文档区分内存修改和已保存快照；生成/加载/保存之间设入口保护；清空后需手动保存才能影响下次恢复。pending 加载时转 failed，不恢复旧网络任务。
- 共享校验、存储、preload 和 React 读写示例通过严格类型检查。临时目录测试通过：缺失文件、Unicode 读写、pending 转换且不改原文件、重复 ID/非法长度/未知版本、损坏文件拒写、失败后队列恢复、连续保存顺序和临时文件清理。
- 本次只更新文档；未改应用源码，未读取或修改真实聊天和密钥文件，没有调用模型。实际窗口与磁盘集成待学习者实现。
- 下一步：完成第十课保存、重启、缺失/损坏保护与自测；根据结果安排防抖自动保存和退出提醒。

## 第九课反馈与核对（2026-09-15）

- 用户反馈第九课完成并提交八道自测；此前已确认上下文测试误用了底部单轮练习区。
- 本次核对 App 的订阅、固定助手消息、三种终态、停止入口和 MessageList 状态；npm run typecheck 的 Node、Web 两组通过。
- App 仍保留 ModelStreamPractice 导入、showModelStream 状态和底部挂载，课程收尾项尚未完成。未替学习者修改源码。
- 自测讲解：requestId 标识一次生成请求，不是整段对话；assistantId 标识一条助手消息；React key 标识列表项/组件身份。空助手消息让所有 delta 更新同一个目标。
- 自测纠正：history 在追加 pending 消息前创建，避免空助手进入请求、当前问题重复以及误读 React 旧状态快照；done 是成功完成，不是停止指令，cancelled 才是取消终态。
- 自测补充：失败/停止问答不进入本课后续历史；主进程 finally 释放网络与任务资源，React finally 恢复请求标记和界面，不删除消息。禁止请求中清空主要防止旧任务回写已清空的对话。
- 自测纠正：多轮流式文字不等于完整推理状态，也不等于聊天持久化；当前只在 React 内存保存文字，重启丢失，未保存完整 output、推理项和 phase。
- 本次未独立调用网关或操作窗口。下一步巩固上述区别、移除旧练习区挂载，再按后续安排学习本地持久化。

## 第九课安排（2026-09-15）

- 用户要求创建第九课。本次核对 App、AppAPI、buildContext 和真实流练习区，确认 stop 已改为 cancelModelStream，未独立运行窗口。
- 创建 13-第九课-正式聊天的流式消息与上下文.md：真实流接口改收历史数组，复用 parseHistory；正式 App 订阅片段并更新固定助手占位消息，新增 cancelled 状态。
- 成功才将整轮标 complete；停止/失败保留部分文字但不进入 history。移除独立练习区挂载，同时适配保留文件的新接口以继续通过类型检查。
- 在内存中按课程适配当前 App/main/preload/旧练习组件进行严格类型检查，通过。课程编写时已修正 ModelStreamResult 分支的类型收窄。
- 用课程函数和替代状态更新器做离线检查，验证固定消息 ID、片段追加、旧事件排除、成功、部分取消/失败、历史筛选和清理。不是 React DOM 或 Electron 窗口集成测试。
- 未修改应用源码、读取密钥或调用网关。下一步由用户实现并验收正式多轮流式聊天；第十课计划本地聊天持久化。

## 第八课反馈与核对（2026-09-15）

- 用户反馈第八课完成，提交八道自测回答。
- 已核对 SSE 解码、分帧、事件判断、真实流处理器、preload 和页面调用。npm run typecheck 两组均通过。
- 发现 ModelStreamPractice.tsx 的 stop() 仍调用 cancelMockStream(id)，因此按钮没有取消真实任务；卸载清理处已经使用 cancelModelStream。需由学习者修改停止入口并重新验证。
- 自测纠正：网络块是任意分段字节，SSE 是按空行分隔的事件封装，delta 是事件中的增量文字；TextDecoder stream:true 用于保留跨块未完整的 UTF-8 字节。
- 自测纠正：output_text.done 表示一段文字结束，reader done 表示字节流结束，response.completed 才是本课成功终态；不能重复追加完整 done 文字，也不能把提前断流当成功。
- 自测补充：abort 触发网络读取退出，reader finally 取消并释放锁，主进程 finally 释放任务/计时器/监听器，start Promise 结果再驱动 UI；总超时从本次请求开始计时，无数据不一定是服务器卡住，也可能排队、网络或网关缓冲。
- 已讲解：HTTP 200 后仍需检查 SSE 格式和模型终态；本课只是独立单轮流，未接入正式 messages/history，也没有完整推理状态回放。
- 本次未调用网关或操作窗口，不新增真实停止、错误或卸载验收结论。应用代码留给学习者修正。

## 第八课安排（2026-09-15）

- 用户要求创建第八课。已核对现有 model.ts、共享 API 和后续课程约定。
- 创建 12-第八课-真实流式响应与网络取消.md：新增独立真实流练习区，保留模拟区和正式非流式聊天，使用现有模型与环境配置。
- 已读取官方 HTTP SSE 指南和 Responses 流事件定义，明确 delta、output_text.done、completed、failed、incomplete 与网络 EOF 的区别。
- 示例包括流式 UTF-8 解码、跨块 CRLF/多行 data、资源上限、完成事件校验、reader 取消与锁释放、60 秒总超时、按窗口/请求取消和卸载清理。
- main/preload/按课程复制的 React 示例严格类型检查通过。离线测试覆盖字节拆分、Unicode、CRLF、多行数据、done 不重复追加、错误/空回复/提前断流、reader 主动取消、HTTP/MIME 错误、主动取消、超时、旧 ID、重复启动和窗口监听器清理，均通过。
- 没有修改应用实现、读取本地密钥或调用真实网关；本地测试使用虚构配置和替代网络响应。真实网关和 Electron UI 仍待用户实践。
- 下一步：完成第八课真实完成/停止/失败验收及自测，第九课再设计正式聊天的流式消息状态与上下文整合。

## 第七课反馈与核对（2026-09-15）

- 用户反馈第七课完成，提交八道自测回答。
- 核对 mock-stream.ts、StreamPractice、preload、AppAPI 和注册位置：已有增量事件、结束结果、按窗口管理任务、请求 ID 检查、停止和卸载清理；未发现课程模拟故障行。
- 实际运行 npm run typecheck，Node、Web 均通过。本次未独立操作运行窗口，不将类型检查视为停止、卸载和重复订阅交互已验收。
- 自测讲解：delta 是可多次到达的增量数据，start Promise 是一次性的任务最终结果；先订阅防止遗漏开头片段。
- 自测纠正：函数式 setOutput 的关键是获取更新队列中的前值，避免闭包旧快照丢片段；requestId 标识生成任务，React key 标识组件实例。
- 自测补充：取消订阅只停止页面接收，不停止主进程定时器；finish 统一释放定时器、任务和窗口监听器，再完成 Promise，由 React 更新结束状态。
- 自测讲解：removeListener 依靠函数引用身份匹配注册项；即使新函数代码相同，也不能移除原 handler。点击停止只是请求，最终 cancelled 结果才触发已停止状态；确认前不允许新任务抢跑。
- 下一步：巩固事件与 Promise、函数式更新和监听器引用身份，再进入真实流式接入。

## 第七课安排（2026-09-15）

- 用户要求创建第七课。已核对现有 AppAPI、preload、App 和主进程注册；第六课仍是非流式聊天，页面文案已修正为多轮文本问答。
- 创建 11-第七课-模拟流与停止生成.md。采用独立 StreamPractice 练习区，保留现有真实聊天，避免模拟回复混入历史。
- 课程包含增量事件、最终 Promise 结果、每窗口任务 Map、requestId 匹配、停止、统一 finish 清理、useEffect 订阅和卸载时取消。
- 对课程 main/preload/React 示例做严格类型检查，通过。本地模拟验证正常完成、停止、旧 ID、重复启动、窗口销毁/加载/进程退出、发送失败及非法输入；定时器和窗口监听器清理均通过。
- 本次只更新学习文档，未写入应用实现、未运行真实 Electron 交互或调用模型。
- 下一步：用户亲手实现并完成停止、故障、卸载及重复订阅实验，自测后进入真实流接入。

## 第六课反馈与核对（2026-09-15）

- 用户反馈第六课完成，提交八道自测回答。
- 源码已实现 buildContext、pending/complete/failed 状态、完整问答裁剪、发送时禁止清空及 key 重置输入草稿；主进程已使用数组校验，旧字符串校验已注释，不再执行。
- 实际运行 npm run typecheck，Node、Web 两组均通过。本次未独立运行窗口或调用模型网关，不新增网络、裁剪或清空交互验收结论。
- 自测纠正：messages 是页面状态和上下文来源，history 是本次派生的请求快照；setMessages 安排更新，不会替换当前函数闭包里的 messages；消息 status 记录每次请求结果，全局 isSending 只表示当前是否忙碌。
- 自测补充：本课使用 fetch HTTP 非流式请求，没有实现 WebSocket；失败问题本身可以完整，但未形成成功问答，本课规则将它排除；整轮裁剪保留问答关系，role 已说明说话方。
- 已讲解：按钮 disabled 负责界面交互，sendingRef 负责函数入口同步检查；key 标识 React 组件实例，不等于业务对话 ID；改变 ChatInput 的 key 重置其草稿，不重置 App 的目录状态。
- 已讲解：store: false 不会阻止每次显式发送文本历史；文本上下文不等于完整推理状态。第六课按教学范围只重建文本。
- 剩余整理：App 的“模拟回复”文案仍过时，可改为“多轮文本问答”；旧字符串校验注释可删除。应用代码继续由学习者亲手维护。
- 下一步：巩固状态快照、请求状态和 key 的含义，再进入模拟流、订阅清理和主动停止。

## 第五课收尾与第六课安排（2026-09-15）

- 用户要求创建第六课，继续由学习者亲手实现。
- 源码核对：App 已补充等待和错误提示 JSX；重新执行 npm run typecheck，两组检查均通过，先前 chatError 未使用错误已解决。页面“模拟回复”介绍仍待替换。
- 已创建 10-第六课-多轮上下文与清空对话.md，包含数组接口、边界校验、消息完成状态、最近 4 轮完整问答与总长度裁剪、请求快照、清空对话和用 key 重置草稿。
- 已读取官方会话状态和 phase 文档；本课限定为重建文本上下文，明确不等于完整推理状态回放。现有模型、网关和 store: false 保留。
- 对课程中的 buildContext、parseHistory 及对应类型做严格类型检查和本地边界验证，验证空历史、轮数裁剪、失败/pending 排除、超长裁剪、原数组不变、合法及非法参数，均通过。
- 未修改应用实现、未读取密钥、未调用网关；完整第六课交互与网络验收仍待用户实践。
- 下一步：实现第六课并反馈第二轮 history、失败恢复、清空与裁剪实验和自测；第七课计划模拟流与停止、订阅清理。

## 第五课反馈与核对（2026-09-15）

- 用户反馈第五课完成，提交八道自测回答。
- 已核对 model.ts、共享类型、preload、App 与 ChatInput。主进程实现了输入检查、HTTP 请求、响应解析、30 秒超时和 finally 清理；App 已替换模拟回复逻辑并设置发送状态。
- 实际运行 npm run typecheck：Node 通过；Web 报 TS6133，App.tsx 的 chatError 声明后未读取。页面缺少 chatError 错误提示及单独的等待提示；ChatInput 的 input 缺少 disabled 和 maxLength；页面介绍仍称使用模拟回复。
- 自测：第八题“没有把上下文给模型”正确；第一题需区分父组件回调与跨进程 IPC；第二至七题需巩固 HTTP 成功、业务失败与 Promise 拒绝、运行时输入校验、state/ref 和 finally/AbortController 的职责。
- 已讲解：isSending 驱动等待 UI，sendingRef 同步防重复进入；return 离开 try 前仍执行 finally；计时器触发 abort，finally 清理计时器与 busy，成功也需清理。
- 验证边界：未读取密钥配置，未调用模型网关，未独立运行错误实验；不将用户笼统完成反馈等同于所有验收项通过。应用代码留给学习者修正。
- 下一步：补齐 JSX 提示与输入属性、修改过时文案，再通过类型检查并验证失败后恢复；随后复述核心自测概念。

## 第四课收尾与第五课安排（2026-09-15）

- 用户确认第四课没问题，要求创建第五课。
- 重新运行 npm run typecheck，两组检查均通过，先前 TS5090 已解决。
- 核对现有 App、ChatInput、共享 AppAPI、preload 和主进程；聊天仍使用模拟回复。
- 用户指定 gpt-5.5，第三方 base_url 为 https://www.gmapikey.com，协议为 Responses；本课保留指定模型，显式发送 store: false。
- 已创建 09-第五课-真实模型请求与异步状态.md，同步导航与路线。未替用户修改应用实现，也未调用模型服务。
- 文档覆盖开发配置、类型化业务结果、HTTP 状态检查、Responses 返回值检查、等待与重复提交、超时清理和错误实验。
- 已提醒用户撤销并更换在聊天中暴露的密钥；文档不记录密钥。
- 下一步：由用户完成第五课；验证网关实际完整路径、非流式兼容性和真实回复，再反馈自测与错误实验。

## 待解答问题

暂未登记。学习时保留自己的原始问题，解决后补充对应文件和解释。

## 第四课反馈与核对（2026-09-15）

- 用户反馈第四课完成，提交八道自测回答。
- 源码核对：已实现共享 AppAPI、main 目录对话框处理器、preload 桥接和返回值检查、Window 声明，以及 React 的选择、取消保留路径和异常恢复按钮逻辑。
- 实际执行 npm run typecheck：Node 检查通过；Web 检查失败，tsconfig.web.json 的 paths 使用 src/renderer/src/*，未设置 baseUrl，报 TS5090。可将该映射值改为 ./src/renderer/src/*，再重新检查；留给学习者亲手修改。
- 自测纠正：onSend 是同一进程内的普通回调，跨进程需要 IPC；send 不直接返回处理结果，invoke 返回 Promise；preload/index.ts 实现桥接，index.d.ts 只描述类型；仅有声明而无实现时，调用缺失方法会出现运行时 TypeError。
- 自测补充：取消返回 null，跳过路径更新以保留旧值；isSelecting 表示等待状态并减少重复点击；unknown 表示类型未知，as 是类型断言，不进行运行时检查或转换；重启后路径重置是因为只有 React 内存状态，尚未持久化。
- 验证边界：本次没有观察运行窗口，不将选择、取消、模拟错误或断点实验标记为独立验证通过。未修改应用源码。
- 下一步：修正路径配置并重新运行类型检查，巩固自测概念；随后确定第五课模型服务与请求格式。

## 第一课反馈与第二课安排（2026-09-15）

- 用户反馈：VS Code 调试启动问题已解决，完成第一课自测反馈。
- 源码核对：App.tsx 的按钮文字已改为“发送IPC”，主体仍为模板。
- 已讲解的纠正：订阅 ping 的是主进程，终端显示日志；contextBridge 暴露 window.electron，声明文件只补充类型；preload 不是独立第三个进程；out 是编译输出，最终安装包由后续打包生成。
- 已理解的方向：修改界面的位置、输出目录不作为源码修改、仅安装更新依赖不等于实现更新。
- 未单独报告：pong 日志或断点命中证据，不代替用户标记通过。
- 下一步：先完成第二课单文件版本，再拆 MessageList；提交类型检查、交互验收和自测结果。

## 类型声明专题与本次进度更新（2026-09-15）

- 已完成：第一课自测反馈及概念纠正；用户确认 VS Code 调试启动问题已解决。
- 已理解：ElectronAPI 是类型，electronAPI 是实际对象。contextBridge.exposeInMainWorld 在运行时向页面暴露对象。
- 已理解：index.d.ts 中的 interface Window 扩展已有 Window 类型，不创建变量或对象；它更接近 C++ 的声明，而不是宏替换。
- 已理解：类型声明供静态检查和编辑器提示使用，编译后被移除；不会进行运行时验证，也不要求先执行声明再赋值。
- 已纠正：不是“把类型赋值给 window.electron”，而是“暴露实际对象，并用类型描述其接口”。
- IPC 复习：主进程订阅 ping，终端只是显示主进程日志；send/on 发送事件，invoke/handle 用于请求结果。
- 调试问题记录：使用 Node.js 单独运行 preload 或 main，分别出现 contextBridge 导入错误和 PNG 扩展名错误。应从项目根目录的 VS Code 调试配置选择 Debug All，由 electron-vite 启动完整应用。
- 验收边界：理论讨论已完成；本次没有新执行应用检查，不把未报告的 pong 日志或断点命中标记为已验证。
- 下一步：进入已创建的第二课，亲手实现 React + TypeScript 最小聊天交互；实践时顺便补记第一课的 pong 观察结果。

## 第二课反馈与第三课安排（2026-09-15）

- 用户确认第二课完成，要求开始第三课。
- 实际代码：App 有受控输入、函数式追加消息、模拟回复；MessageList 使用 Props 和稳定 key；用户扩展了 system 角色。
- 待修正代码：system 目前会落入“助手”标签分支，第三课通过角色映射练习处理。
- 已纠正概念：useState 是组件内存状态，不是浏览器持久缓存；重新渲染保留同一实例的状态，刷新页面和卸载后重新挂载会重新初始化。用户已复述重新渲染与刷新区别；卸载情况在第三课用实验巩固。
- 第二课其余自测已讲解：受控输入、字面量联合类型、函数式更新、稳定 key、单一状态来源，以及无需 Effect/IPC 的原因。
- 验证边界：完成状态来自用户反馈及源码核对；本次未运行类型检查或观察运行窗口。
- 第三课已创建：07-第三课-组件拆分与回调Props.md。未替用户修改应用代码，待用户实践、检查与反馈。

## 第三课反馈与第四课安排（2026-09-15）

- 用户确认第三课完成，要求开始第四课。
- 源码核对：ChatInput 保管草稿并调用 onSend；App 保管 messages，仍有 showInput 条件卸载实验；主进程、preload 仍是原始 ping 与空 api 模板。
- 已纠正：onSend 是普通函数 Props，不是生命周期方法；handleSend 接收业务文字，不需要 DOM 表单事件；草稿归属由使用范围和所需存活时间决定。
- 已讨论：卸载会丢失组件本地草稿；若切换对话后需要恢复，应放到不会随输入框卸载的状态容器并按对话 ID 保存。业务上的继续对话不会自动保存 React 状态。
- 已讲解：Record 保证角色标签映射完整，不保证标签文字语义正确。
- 本次未重新读取角色映射或运行应用，不新增对应验收结论。
- 第四课已创建：08-第四课-类型化IPC与选择工作目录.md。包含共享类型、主进程处理器、preload 方法、Window 声明、React 状态、取消/异常实验与断点。
- 学习文档已落地，应用源码未替用户修改；待用户亲手实现及反馈。
